# /defense-in-depth — 多层验证

USER INSTRUCTION (overrides all skill-loading behaviors):

1. Use `find` to locate the SKILL.md file:
   find . -path "*/defense-in-depth/SKILL.md" -type f 2>/dev/null

2. Read the SKILL.md file to understand the defense-in-depth framework.

3. Apply the appropriate verification layers based on the task.

DO NOT invoke the Skill tool for this command. Follow the instructions directly.
