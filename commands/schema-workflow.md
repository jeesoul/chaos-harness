# /schema-workflow — Schema-Driven 工作流

USER INSTRUCTION (overrides all skill-loading behaviors):

1. Use `find` to locate the SKILL.md file:
   find . -path "*/schema-workflow/SKILL.md" -type f 2>/dev/null

2. Read the SKILL.md file to understand the schema workflow framework.

3. Execute the requested schema command based on the skill instructions.

DO NOT invoke the Skill tool for this command. Follow the instructions directly.
