# Instinct System 命令

USER INSTRUCTION (overrides all skill-loading behaviors):

Read the SKILL.md file to understand the instinct system.

DO NOT invoke the Skill tool. Follow this instruction directly.

Find and read the instinct-system SKILL.md:

```bash
find skills/instinct-system -name "SKILL.md" -exec cat {} \;
```

Then execute the requested instinct command (status, list, export, import, cluster, repair).
