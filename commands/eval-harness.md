# /eval-harness — 评测驱动开发

USER INSTRUCTION (overrides all skill-loading behaviors):

1. Use `find` to locate the SKILL.md file:
   find . -path "*/eval-harness/SKILL.md" -type f 2>/dev/null

2. Read the SKILL.md file to understand the eval framework.

3. Execute the requested eval command based on the skill instructions.

DO NOT invoke the Skill tool for this command. Follow the instructions directly.
