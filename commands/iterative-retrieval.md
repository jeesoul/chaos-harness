# /iterative-retrieval — 迭代检索模式

USER INSTRUCTION (overrides all skill-loading behaviors):

1. Use `find` to locate the SKILL.md file:
   find . -path "*/iterative-retrieval/SKILL.md" -type f 2>/dev/null

2. Read the SKILL.md file to understand the iterative retrieval framework.

3. Execute the requested retrieval command based on the skill instructions.

DO NOT invoke the Skill tool for this command. Follow the instructions directly.
