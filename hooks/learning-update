#!/bin/sh
# learning-update - PostToolUse hook for Write|Edit operations
# Records learnings from operations

DATA_DIR="$HOME/.claude/harness"
LEARNING_LOG="$DATA_DIR/learning-log.json"

mkdir -p "$DATA_DIR"
[ ! -f "$LEARNING_LOG" ] && printf '[]\n' > "$LEARNING_LOG"

exit 0